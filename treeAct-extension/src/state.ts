import { NewTreeNode } from "../../common/types";

// eslint-disable-next-line prefer-const
export let treeState: NewTreeNode[] = [
  {
    id: 0,
    name: "App",
    children: [],
    isExpanded: true,
  },
];
